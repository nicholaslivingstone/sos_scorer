from enum import Enum, auto

class Skull(Enum): 
    ROYAL = auto()
    PEASANT = auto()
    PRIEST = auto()
    ROMANTIC = auto()
    ROMANCED = auto() # Romantic that's already been checked
    CRIMINAL = auto()
